[build]
target = "index.html"
dist = "dist"
release = true

[watch]
ignore = ["dist", "target"]

[[hooks]]
stage = "post_build"
command = "sh"
command_arguments = ["-c", "cp -r public/* dist/ 2>/dev/null || true"]
