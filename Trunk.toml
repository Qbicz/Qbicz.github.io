[build]
target = "index.html"
dist = "dist"
public_url = "/"

[[hooks]]
stage = "post_build"
command = "sh"
command_arguments = ["-c", "cp -r public/* dist/ 2>/dev/null || true; cp dist/index.html dist/404.html 2>/dev/null || true"]
